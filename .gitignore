*.iml
.gradle/
/local.properties
# IntelliJ/Android Studio editor state — regenerated locally, not shared
.idea/
.DS_Store
# Gradle build output — matches a "build" dir at any depth (app/, core/*/, feature/*/, asr/*/, build-logic/*/, …)
build/
/captures
.externalNativeBuild
.cxx
local.properties
sherpa-onnx aar
third_party/m2/
keystore.properties
*.jks
*.keystore
jotjive-legal/

# Release build bundles / installables — never commit these
*.aab
*.apk